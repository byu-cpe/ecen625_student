compile:
	clang-9 -c -emit-llvm $(NAME).c -o $(NAME).clang.bc
	llvm-dis-9 $(NAME).clang.bc 
	opt-9 -load $(LEVEL)/../build/scheduler_625.so -analyze -memdep -sched625 $(NAME).clang.bc

clean:
	rm -rf *.bc 